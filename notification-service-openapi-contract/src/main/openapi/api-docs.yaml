openapi: 3.0.1
info:
  title: OpenAPI definition
  version: v0
servers:
- url: http://notification:8080
  description: Generated server url
paths:
  /notifications/shipment-created:
    post:
      tags:
      - notification-controller
      operationId: notifyShipmentCreated
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentNotificationRequest"
        required: true
      responses:
        "200":
          description: OK
components:
  schemas:
    ShipmentNotificationRequest:
      type: object
      properties:
        email:
          type: string
        externalId:
          type: string
        trackingNumber:
          type: string
